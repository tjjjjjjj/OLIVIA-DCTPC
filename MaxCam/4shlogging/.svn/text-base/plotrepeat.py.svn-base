import sys
import time
import pylab
import DmtpcLogViewingUtils as dlvu

if len(sys.argv) > 1:
    npoints = int(sys.argv[1])
else:
    npoints = 500
    
print "npoints = ", npoints

while (True):
    dlvu.getBPG(npoints=npoints, autoupdate=True)

